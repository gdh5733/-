# 应用端口号
server:
  port: 8765

# 配置spring的应用名
spring:
  application:
    name: eureka

# 默认不注册自己
eureka:
  client:
    register-with-eureka: false

    # 手动配置地址
    service-url:
      defaultZone: http://eureka2:8766/eureka/,http://eureka3:8767/eureka/